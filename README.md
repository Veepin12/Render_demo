feat: add Flask ML placement prediction web app

- Logistic Regression model (model.pkl) trained on 4 features, 3 classes
- placement.pkl for placement-specific predictions
- Flask app with / (home) and /predict (POST) routes
- requirements.txt with full dependency list (scikit-learn, Flask, gunicorn)
- .gitignore to exclude unnecessary files
